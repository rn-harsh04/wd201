<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>"/>
    <title>Todo App</title>
    <link rel="stylesheet" href="./css/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <div class="grid grid-cols-6 min-h-screen">
        <div class="col-start-2 col-span-4 lg:col-start-3 lg:col-span-2 p-4">
            <%- include('header.ejs') %>
            <p class="mb-3 text-gray-500 dark:text-gray-400 font-semibold">Welcome to the TO-Do manager </p>
            <p class="mb-3 text-gray-500 dark:text-gray-400">New Here? <a href="/signup" class="font-medium text-blue-600 underline dark:text-blue-500 hover:no-underline">Sign-Up-now </a></p>
            <p class="mb-3 text-gray-500 dark:text-gray-400">Already have an account <a href="/login" class="font-medium text-blue-600 underline dark:text-blue-500 hover:no-underline">Sign-in</a></p>
            <%- include('footer.ejs') %>
        </div>
    </div>
</body>
</html>